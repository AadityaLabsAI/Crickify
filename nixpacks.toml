# Cricket Bot - Railway/Nixpacks Configuration
# This is the MINIMAL config that lets Nixpacks handle everything automatically
# DO NOT add custom install commands - they break NixOS package management

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
PYTHONUNBUFFERED = "1"
